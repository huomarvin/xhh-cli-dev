// const { validateCommitMsg } = require("../lib");

// test('validateCommitMsg "foo: bar"', async () => {
//   const report = await validateCommitMsg("foo: bar");
//   expect(report.valid).toBeFalsy();
// });

// test('validateCommitMsg "feat: add"', async () => {
//   const report = await validateCommitMsg("feat: add");
//   expect(report.valid).toBeTruthy();
// });

test(() => {
  expect(1).toBe(1);
});
